{
  "name": "train-scheduling-booking-system",
  "version": "1.0.0",
  "description": "Web-based Train Scheduling & Booking System — Monorepo Root",
  "private": true,
  "scripts": {
    "dev": "concurrently -n BACKEND,FRONTEND -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "test": "npm run test --prefix backend",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}

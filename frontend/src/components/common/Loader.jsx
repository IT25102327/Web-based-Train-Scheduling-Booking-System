/**
 * Loader Component
 * Loading indicator spinner component.
 *
 * @component
 * @returns {React.ReactElement} Loader component placeholder
 */
import React from 'react';

const Loader = () => {
  return (
    <div className="loader">
      Loader Component
    </div>
  );
};

export default Loader;

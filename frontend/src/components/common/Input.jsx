/**
 * Input Component
 * Reusable input component for text, email, and password input.
 *
 * @component
 * @returns {React.ReactElement} Input component placeholder
 */
import React from 'react';

const Input = () => {
  return (
    <div className="input">
      Input Component
    </div>
  );
};

export default Input;

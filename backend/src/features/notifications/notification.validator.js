/**
 * @file notification.validator.js
 * @description Request validation rules for notification endpoints.
 */

const validateNotificationInput = (req, res, next) => {
  // TODO: Validate notification parameters
  next();
};

module.exports = {
  validateNotificationInput,
};

/**
 * @file booking.validator.js
 * @description Request validation rules for booking and payment endpoints.
 */

const validateCreateBooking = (req, res, next) => {
  // TODO: Validate booking creation parameters
  next();
};

module.exports = {
  validateCreateBooking,
};

/**
 * @file passenger.validator.js
 * @description Request validation rules for passenger endpoints.
 */

const validateUpdateProfile = (req, res, next) => {
  // TODO: Validate passenger profile update payload
  next();
};

module.exports = {
  validateUpdateProfile,
};
